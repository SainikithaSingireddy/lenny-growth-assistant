import SandboxedIframe from "./SandboxedIframe";

interface Props {
  html: string;
}

export default function ArtifactViewer({ html }: Props) {
  if (!html) return null;

  return (
    <div>
      <h3 style={{ marginTop: 0 }}>Artifact Viewer</h3>

      <p style={{ color: "#64748b", fontSize: 14 }}>
        HTML report generated by the assistant.
      </p>

      <SandboxedIframe html={html} />
    </div>
  );
}